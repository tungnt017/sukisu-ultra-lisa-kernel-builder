--- a/fs/stat.c
+++ b/fs/stat.c
@@ Description: Hook vfs_statx() or vfs_fstatat()

 Insert hook BEFORE the first if-statement — after declarations.

+#ifdef CONFIG_KSU
+extern int ksu_handle_stat(int *dfd, const char __user **filename_user, int *flags);
+#endif

 /* For kernels WITH vfs_statx: */
 int vfs_statx(int dfd, const char __user *filename, int flags, ...)
 {
 	/* declarations */
+#ifdef CONFIG_KSU
+	ksu_handle_stat(&dfd, &filename, &flags);
+#endif
 	if ((flags & ~(...)) != 0)

 /* For kernels WITHOUT vfs_statx: */
 int vfs_fstatat(int dfd, const char __user *filename, struct kstat *stat, int flag)
 {
 	/* declarations */
+#ifdef CONFIG_KSU
+	ksu_handle_stat(&dfd, &filename, &flag);
+#endif
 	if ((flag & ~(...)) != 0)
